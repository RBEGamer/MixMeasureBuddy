apiVersion: v1
kind: Service
metadata:
  name: mixmeasurebuddy-suite
spec:
  selector:
    app: mixmeasurebuddy-suite   # matches the pod’s labels
  ports:
    - name: http
      port: 80        # cluster‑internal port
      targetPort: 8082  # proxy container port
  type: ClusterIP